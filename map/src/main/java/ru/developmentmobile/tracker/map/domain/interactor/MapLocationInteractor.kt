package ru.developmentmobile.tracker.map.domain.interactor

import ru.developmentmobile.tracker.map.domain.model.MapLocation
//import ru.developmentmobile.tracker.map.domain.repository.MapRepository

class MapLocationInteractor (
//    private val mapRepository: MapRepository
) {

//    suspend fun getLocations() = mapRepository.getLocations()

//    suspend fun addSingleLocation(location: MapLocation) = mapRepository.addSingleLocation(location)

//    suspend fun deleteLocation(locationId: Int) = mapRepository.deleteLocation(locationId)

//    suspend fun deleteLocations() = mapRepository.deleteLocations()
}
